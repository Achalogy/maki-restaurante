info:
  name: deliveries
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/delivery"
  auth: inherit

params:query:
  active: 1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
