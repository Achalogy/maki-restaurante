info:
  name: Untitled
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/delivery?active=1"
  params:
    - name: active
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
