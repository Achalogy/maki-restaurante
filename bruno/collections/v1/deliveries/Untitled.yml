info:
  name: Untitled
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/delivery"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
