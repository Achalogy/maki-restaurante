info:
  name: aditional categories
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/api/v1/aditional?categoryId=2"
  params:
    - name: categoryId
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
