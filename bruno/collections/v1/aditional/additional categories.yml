info:
  name: additional categories
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/api/v1/additional/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
