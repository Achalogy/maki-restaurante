info:
  name: category by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/v1/aditional/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
