info:
  name: additionals
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/additional"
  params:
    - name: categoryId
      value: "1"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
