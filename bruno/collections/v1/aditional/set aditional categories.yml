info:
  name: set aditional categories
  type: http
  seq: 6

http:
  method: POST
  url: "{{host}}/api/v1/aditional"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
