info:
  name: set aditional categories
  type: http
  seq: 6

http:
  method: POST
  url: "{{host}}/api/v1/aditional/1/categories"
  body:
    type: json
    data: |-
      [
        {
          "id": 1
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
