info:
  name: aditionals
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/aditional?categoryId=2"
  params:
    - name: categoryId
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
