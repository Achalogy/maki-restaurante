info:
  name: aditional
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/api/v1/aditional"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
