info:
  name: delete purchase order
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{host}}/api/v1/purchase-order/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
