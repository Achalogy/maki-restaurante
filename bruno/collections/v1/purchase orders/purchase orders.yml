info:
  name: purchase orders
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/purchase-order?notCompleted=1"
  params:
    - name: notCompleted
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
