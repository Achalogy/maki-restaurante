info:
  name: create purchase order
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/api/v1/purchase-order/client/1"
  auth: inherit
  body: json

body:json:
  [
    {
      "id": 1,
      "additionals": []
    }
  ]

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
