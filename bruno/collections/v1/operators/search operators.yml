info:
  name: search operators
  type: http
  seq: 8

http:
  method: GET
  url: "{{host}}/api/v1/operator/search?term=john"
  auth: inherit

params:query:
  term: john

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
