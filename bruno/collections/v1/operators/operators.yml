info:
  name: operators
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/operator"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
