info:
  name: operator by id
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/api/v1/operator/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
