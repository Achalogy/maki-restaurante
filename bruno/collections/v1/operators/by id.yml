info:
  name: by id
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{host}}/api/v1/operator/21"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
