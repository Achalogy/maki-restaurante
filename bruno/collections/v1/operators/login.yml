info:
  name: login
  type: http
  seq: 11

http:
  method: POST
  url: "{{host}}/api/v1/operator/log-in"
  auth: inherit
  body: json

body:json:
  {
    "username": "operatoruser",
    "password": "password123"
  }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
