info:
  name: count operators by username
  type: http
  seq: 9

http:
  method: GET
  url: "{{host}}/api/v1/operator/count/john_doe"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
