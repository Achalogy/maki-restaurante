info:
  name: operator by username
  type: http
  seq: 7

http:
  method: GET
  url: "{{host}}/api/v1/operator/username/john_doe"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
