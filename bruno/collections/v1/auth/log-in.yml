info:
  name: Auth Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/api/v1/auth/log-in"
  body:
    type: json
    data: |+
      {
        "username": "eveyzoe",
        "password": "1234"
      }


  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
