info:
  name: plate by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/v1/plate/50"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
