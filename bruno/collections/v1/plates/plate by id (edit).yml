info:
  name: plate by id (edit)
  type: http
  seq: 6

http:
  method: POST
  url: "{{host}}/api/v1/plate/1"
  body:
    type: json
    data: |-
      {
        "name": "adf"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
