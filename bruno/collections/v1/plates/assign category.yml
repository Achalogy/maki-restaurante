info:
  name: assign category
  type: http
  seq: 5

http:
  method: POST
  url: "{{host}}/api/v1/plate/50/category/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
