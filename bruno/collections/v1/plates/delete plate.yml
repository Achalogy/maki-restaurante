info:
  name: delete plate
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{host}}/api/v1/plate/49"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
