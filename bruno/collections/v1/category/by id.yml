info:
  name: by id
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{host}}/api/v1/category/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
