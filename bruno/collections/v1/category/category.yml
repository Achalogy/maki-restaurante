info:
  name: category
  type: http
  seq: 4

http:
  method: POST
  url: "{{host}}/api/v1/category"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
