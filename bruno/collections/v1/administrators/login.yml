info:
  name: login
  type: http
  seq: 6

http:
  method: POST
  url: "{{host}}/api/v1/admin/log-in"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
