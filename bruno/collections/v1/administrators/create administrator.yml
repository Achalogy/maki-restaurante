info:
  name: create administrator
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/api/v1/admin"
  auth: inherit
  body: json

body:json:
  {
    "name": "Admin Name",
    "username": "adminuser",
    "password": "password123"
  }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
