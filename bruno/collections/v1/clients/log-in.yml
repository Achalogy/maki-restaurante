info:
  name: log-in
  type: http
  seq: 6

http:
  method: POST
  url: "{{host}}/api/v1/client/log-in"
  body:
    type: json
    data: |-
      {
        "email": "acha@acha.dev",
        "password": "eveyzoe"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
